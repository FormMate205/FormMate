import type { ClassNames } from 'react-day-picker';

export const calendarCustom: Partial<ClassNames> = {
    day: 'relative rounded-full w-9 h-9 flex items-center justify-center text-sm hover:bg-gray-100',
};
